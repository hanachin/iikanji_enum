<%= form_with(model: published_post, local: true) do |form| %>
  <% if published_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(published_post.errors.count, "error") %> prohibited this published_post from being saved:</h2>

      <ul>
        <% published_post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type %>
  </div>

  <div class="field">
    <%= form.label :state %>
    <%= form.select :state, Post.states.keys.map { [_1, _1 ] } %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
