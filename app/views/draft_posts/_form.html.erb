<%= form_with(model: draft_post, local: true) do |form| %>
  <% if draft_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(draft_post.errors.count, "error") %> prohibited this draft_post from being saved:</h2>

      <ul>
        <% draft_post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type %>
  </div>

  <div class="field">
    <%= form.label :state %>
    <%= form.select :state, Post.states.keys.map { [_1, _1 ] } %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
